<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="login">
    <insert id="individualRegister">
        INSERT INTO member(id, passwd, name, email)
        VALUES(#{id}, #{passwd}, #{name}, #{email})
    </insert>

    <insert id="companyRegister">
        INSERT INTO company_member(id, passwd, name, email, phone, corporate_name)
        VALUES(#{id}, #{passwd}, #{name}, #{email}, #{phone}, #{corporateName})
    </insert>

    <select id="individualItem" resultType="Member">
        SELECT *
        FROM member
        WHERE id=#{id}
    </select>

    <select id="companyItem" resultType="CompanyMember">
        SELECT *
        FROM company_member
        WHERE id=#{id}
    </select>


</mapper>
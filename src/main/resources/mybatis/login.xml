<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="login">
    <insert id="register">
        INSERT INTO member(id, passwd, name, phone, nick_name, email, post_code, address, detail_address)
        VALUES(#{id}, #{passwd}, #{name}, #{phone}, #{nickName}, #{email}, #{postCode}, #{address}, #{detailAddress})
    </insert>

    <select id="checkId" resultType="String">
        SELECT id
        FROM member
        WHERE id=#{id}
    </select>

    <select id="checkEmail" resultType="Member">
        SELECT *
        FROM member
        WHERE email=#{email}
    </select>
</mapper>